# 财报分析前端界面 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 financial_report_fetcher 新增本地 Web 前端（FastAPI + 原生 JS 单页），支持股票搜索自动补全、财报查询/预览、AI 分析、自由问答。

**Architecture:** 新增 `webapp/` 包：`server.py` 提供 REST API 并托管静态页；`autocomplete.py` 基于 CNINFO 索引做三级匹配补全；`tasks.py` 用单工作线程串行执行长任务（分析），前端轮询状态；复用既有 `CNINFODatasource` / `ReportAnalyzer` / `ReportDownloader` / `AIClient`，仅给 `ReportAnalyzer` 做加法式小改动（显式元数据参数 + 非交互问答方法）。

**Tech Stack:** FastAPI + uvicorn（新）、原生 HTML/JS/CSS（无构建工具）、pypdf（新，analyzer 依赖）、pytest + httpx TestClient。

**Spec:** `docs/superpowers/specs/2026-08-03-财报分析前端界面-design.md`

**注意：** 项目当前不是 git 仓库。Task 1 会 `git init`（本地仓库、无 remote；下载的 PDF 等产物通过 .gitignore 排除）。若你不需要 git，跳过 Task 1 的 commit 步骤即可。

---

### Task 0: git 仓库初始化

**Files:**
- Create: `.gitignore`

- [ ] **Step 1: 初始化 git 仓库**

```bash
cd gp-agent
git init
```

Expected: `Initialized empty Git repository in .../.git/`

- [ ] **Step 2: 创建 .gitignore**

```gitignore
__pycache__/
*.pyc
.pytest_cache/
.hypothesis/
reports/
.venv/
venv/
```

- [ ] **Step 3: 首次提交**

```bash
git add .
git commit -m "chore: init repo with existing project and design spec"
```

---

### Task 2: analyzer 显式元信息 + 非交互问答方法

**Files:**
- Modify: `financial_report_fetcher/analyzer.py:250-356`（`analyze` 签名与元信息解析）
- Modify: `financial_report_fetcher/analyzer.py:360-413`（新增 `qa` 方法）
- Test: `tests/unit/test_analyzer.py`（新建）
- Modify: `docs/superpowers/specs/2026-08-03-财报分析前端界面-design.md`（3.3 措辞同步）

背景：Web 端传文件路径即可，但旧 `_parse_meta` 无法从新文件名 `长江电力_600900_年报_2025.pdf` 正确解析 ticker（正则会把「长江电力」当代码）。故 `analyze()` 增加可选 `meta` 字典跳过文件名解析；另新增非交互式 `qa()`（CLI 的 `chat()` 是 stdin 交互，Web 不可用）。

- [ ] **Step 1: 写失败测试** `tests/unit/test_analyzer.py`

```python
"""ReportAnalyzer 单元测试（Web 端新增能力）"""

from datetime import date
from unittest.mock import MagicMock

import pytest

from financial_report_fetcher.analyzer import ReportAnalyzer


def _mock_client():
    client = MagicMock()
    client.default_model = "test-model"
    client.chat.return_value = {
        "content": "分析结果",
        "model": "test-model",
        "usage": {"total_tokens": 10},
    }
    return client


class TestAnalyzeWithMeta:
    def test_meta_overrides_filename_parsing(self):
        """meta 传入时跳过文件名正则解析（文件名可以是任意字符串）"""
        client = _mock_client()
        analyzer = ReportAnalyzer(client)
        # 文件名不含任何可解析的 ticker/年份
        report = analyzer.analyze(
            "whatever.pdf",
            dimensions=["financial_summary"],
            meta={"ticker": "600900", "year": 2025, "company": "长江电力"},
        )
        assert "长江电力" in report.company
        assert "600900" in report.company
        assert report.report_year == 2025
        assert report.dimensions[0].content == "分析结果"

    def test_meta_absent_keeps_filename_parsing(self):
        """不传 meta 时保持原文件名解析行为（回归）"""
        client = _mock_client()
        analyzer = ReportAnalyzer(client)
        report = analyzer.analyze(
            "600519_年报_2024.pdf",
            dimensions=["financial_summary"],
        )
        assert report.report_year == 2024
        assert "600519" in report.company


class TestQA:
    def test_qa_returns_chat_content_with_history(self):
        """qa() 构造带 system 上下文与历史的 messages，返回模型回复"""
        client = _mock_client()
        analyzer = ReportAnalyzer(client)
        history = [{"role": "user", "content": "问1"}, {"role": "assistant", "content": "答1"}]
        answer = analyzer.qa("whatever.pdf", question="现金流如何？", history=history)
        assert answer == "分析结果"
        # 断言 messages 结构：system + 历史 + 当前问题（共 4 条）
        sent = client.chat.call_args.kwargs["messages"]
        assert sent[0]["role"] == "system"
        assert sent[1:3] == history
        assert sent[3] == {"role": "user", "content": "现金流如何？"}

    def test_qa_raises_on_empty_pdf(self):
        """PDF 无可抽取文字时报错"""
        client = _mock_client()
        analyzer = ReportAnalyzer(client)
        # 该方法调用前需要真实 PDF；用空文件验证错误路径
        import tempfile, os
        with tempfile.NamedTemporaryFile(suffix=".pdf", delete=False) as f:
            f.write(b"")
            path = f.name
        try:
            with pytest.raises(ValueError):
                analyzer.qa(path, question="x")
        finally:
            os.unlink(path)
```

- [ ] **Step 2: Run & verify it fails**

Run: `python -m pytest tests/unit/test_analyzer.py -v`
Expected: FAIL — `TypeError: analyze() got an unexpected keyword argument 'meta'`，`ReportAnalyzer.qa` 不存在

- [ ] **Step 3: 修改 analyze() 签名与元信息解析**（[analyzer.py:250-285](financial_report_fetcher/analyzer.py#L250-L285) 附近）

```python
    def analyze(
        self,
        pdf_path: str,
        dimensions: Optional[List[str]] = None,
        model: Optional[str] = None,
        max_chars: int = 15000,
        meta: Optional[Dict[str, Any]] = None,
    ) -> AnalysisReport:
        """
        ...
        Args:
            ...
            meta:   可选显式元信息 {"ticker": str, "year": int, "company": str}。
                   传入时跳过对文件名的解析（Web 端使用，新文件名格式下
                   原正则解析不完整）；缺省时维持原文件名解析行为。
        """
        if dimensions is None:
            dimensions = self.DEFAULT_DIMENSIONS

        logger.info("开始分析 %s [维度=%s, model=%s]", pdf_path, dimensions, model or self.client.default_model)

        # 第一步：提取 PDF 文本
        pdf_text = self._extract_pdf_text(pdf_path, max_chars=max_chars)
        if not pdf_text.strip():
            raise ValueError(f"PDF 文件为空或无法抽取文字：{pdf_path}")

        # 第二步：解析元信息（代码 + 年份 + 中文名称）
        if meta is not None:
            ticker = str(meta.get("ticker") or "")
            report_year = int(meta.get("year") or 0)
            company_name = str(meta.get("company") or "")
        else:
            ticker, report_year = self._parse_meta(pdf_path)
            company_name = self._extract_company_name(pdf_text) or ticker
        display_name = f"{company_name}（{ticker}）" if company_name and company_name != ticker else ticker
```

（`meta` 为 None 时行为与原来完全一致。）

- [ ] **Step 4: 新增 `qa()` 方法**（financial_analyzer.py 中 `chat()` 方法之后）

```python
    # ── 非交互式问答（Web 端）──────────────────────────────

    def qa(
        self,
        pdf_path: str,
        question: str,
        history: Optional[List[Dict[str, str]]] = None,
        max_chars: int = 10000,
        model: Optional[str] = None,
    ) -> str:
        """
        非交互式单轮问答：提取 PDF 文本并带上历史消息请求 AI，
        返回模型回复文本。Web 端每次调用传入累积历史、自行维护会话。

        Args:
            pdf_path:  PDF 文件路径
            question:  用户当前问题
            history:   历史消息 [{"role", "content"}, ...]（不含 system）
            max_chars: PDF 文本截取上限
            model:     模型名，缺省使用 AIClient 默认

        Returns:
            模型回复文本
        """
        pdf_text = self._extract_pdf_text(pdf_path, max_chars=max_chars)
        if not pdf_text.strip():
            raise ValueError(f"PDF 文件为空或无法抽取文字：{pdf_path}")

        messages: List[Dict[str, str]] = [
            {"role": "system", "content": self._build_system_prompt(pdf_text[:10000])}
        ]
        if history:
            messages.extend(history)
        messages.append({"role": "user", "content": question})

        resp = self.client.chat(messages, model=model)
        return resp["content"]
```

- [ ] **Step 5: Run tests & verify pass**

Run: `python -m pytest tests/unit/test_analyzer.py -v` → 4 passed

- [ ] **Step 6: 同步 spec 措辞**（docs/superpowers/specs/...-design.md 中 3.3）

将 3.3 段落替换为：

```markdown
### 3.3 analyzer 小改动（唯一既有代码修改）

`ReportAnalyzer.analyze()` 增加可选 `meta` 参数（`ticker` / `year` / `company`），
传入时跳过文件名正则解析（新文件名 `长江电力_600900_年报_2025.pdf` 下原正则
解析不完整）；新增非交互式 `qa()` 方法供 Web 问答使用（CLI 的 `chat()`
保留不变）。参数缺省时行为与现状一致，不破坏 CLI 与既有测试。
```

- [ ] **Step 7: Run full suite & commit**

Run: `python -m pytest -q` → 158 passed（154 + 新增 4）
Commit: `git add -A && git commit -m "feat(analyzer): support explicit meta and non-interactive qa()"`

---

### Task 3: webapp/autocomplete.py — 股票补全索引

**Files:**
- Create: `webapp/__init__.py`（空文件即可）
- Create: `webapp/autocomplete.py`
- Test: `tests/unit/test_autocomplete.py`

- [ ] **Step 1: Write failing tests** `tests/unit/test_autocomplete.py`

```python
"""StockIndex 自动补全单元测试"""

import threading

import pytest

from webapp.autocomplete import StockIndex

FAKE_COMPANIES = {
    "600900": {"org_id": "1", "name": "长江电力"},
    "600519": {"org_id": "2", "name": "贵州茅台"},
    "000651": {"org_id": "3", "name": "珠海格力电器"},
    "长江电力": {"org_id": "1", "code": "600900"},
    "贵州茅台": {"org_id": "2", "code": "600519"},
    "珠海格力电器": {"org_id": "3", "code": "000651"},
}


class FakeDatasource:
    def build_known_companies(self):
        return FAKE_COMPANIES


def _ready_index():
    idx = StockIndex(datasource=FakeDatasource())
    idx.start()  # 后台线程构建
    idx.wait_ready(timeout=2.0)  # 测试同步等待
    return idx


def test_code_prefix_match():
    idx = _ready_index()
    assert idx.search("6009") == [{"code": "600900", "name": "长江电力"}]


def test_name_prefix_beats_name_contains():
    idx = _ready_index()
    results = idx.search("长江")
    assert results == [{"code": "600900", "name": "长江电力"}]


def test_name_contains_match():
    idx = _ready_index()
    results = idx.search("格力")
    assert results == [{"code": "000651", "name": "珠海格力电器"}]


def test_top_n_limit():
    idx = _ready_index()
    assert len(idx.search("60", limit=1)) == 1


def test_empty_query_returns_empty():
    idx = _ready_index()
    assert idx.search("") == []
    assert idx.search("   ") == []


def test_no_match_returns_empty():
    idx = _ready_index()
    assert idx.search("zzz") == []


def test_company_name_lookup():
    idx = _ready_index()
    assert idx.company_name("600900") == "长江电力"
    assert idx.company_name("999999") is None
```

- [ ] **Step 2: Run & verify fail**

Run: `python -m pytest tests/unit/test_autocomplete.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'webapp'`

- [ ] **Step 3: Implement** `webapp/autocomplete.py`

```python
"""webapp.autocomplete — 股票补全索引

启动时后台线程调用 CNINFODatasource.build_known_companies() 构建
(code, name) 列表（实测约 1 秒）；搜索按三级优先级匹配：
1. 代码前缀（q 为数字）
2. 名称前缀
3. 名称包含
返回 Top N（默认 10）。
"""

import logging
import threading
from typing import Any, Dict, List, Optional, Tuple

logger = logging.getLogger(__name__)


class StockIndex:
    def __init__(self, datasource: Any) -> None:
        self._datasource = datasource          # 提供 build_known_companies()
        self._entries: Optional[List[Tuple[str, str]]] = None  # [(code, name)]
        self._ready = threading.Event()

    # ── 生命周期 ─────────────────────────────────────────────

    def start(self) -> None:
        """后台线程异步构建索引（幂等）"""
        if self._ready.is_set() or self._entries is not None:
            return
        threading.Thread(target=self._build, daemon=True, name="stock-index").start()

    def _build(self) -> None:
        try:
            known = self._datasource.build_known_companies()
        except Exception as exc:
            logger.error("股票索引构建失败：%s", exc)
            self._ready.set()   # 标记结束（entries 仍为 None → 搜索返回空）
            return
        entries = []
        for key, info in known.items():
            if not key.isdigit():
                continue
            name = info.get("name")
            if name:
                entries.append((key, name))
        entries.sort(key=lambda e: e[0])
        self._entries = entries
        self._ready.set()
        logger.info("股票索引构建完成：%d 家", len(entries))

    def wait_ready(self, timeout: float = 5.0) -> bool:
        """等待索引构建完成（Web 启动期间用）。True=就绪，False=超时/失败"""
        return self._ready.wait(timeout) and self._entries is not None

    @property
    def is_ready(self) -> bool:
        return self._entries is not None

    # ── 查询 ─────────────────────────────────────────────────

    def search(self, q: str, limit: int = 10) -> List[Dict[str, str]]:
        q = q.strip()
        if not q or self._entries is None:
            return []

        code_matches, prefix_matches, contains_matches = [], [], []
        for code, name in self._entries:
            if code.startswith(q):
                code_matches.append((code, name))
            elif name.startswith(q):
                prefix_matches.append((code, name))
            elif q in name:
                contains_matches.append((code, name))

        ordered = code_matches + prefix_matches + contains_matches
        # 去重（理论上不重复，防御性保序去重）
        seen, results = set(), []
        for code, name in ordered:
            if code in seen:
                continue
            seen.add(code)
            results.append({"code": code, "name": name})
            if len(results) >= limit:
                break
        return results

    def company_name(self, code: str) -> Optional[str]:
        """按代码查公司名（无则 None）"""
        if self._entries is None:
            return None
        for c, name in self._entries:
            if c == code:
                return name
        return None

    def is_valid_code(self, code: str) -> bool:
        if not code.isdigit() or self._entries is None:
            return False
        return any(c == code for c, _ in self._entries)
```

（`Optional` 导入需补：`from typing import Optional`。）

- [ ] **Step 4: Run tests & verify pass**

Run: `python -m pytest tests/unit/test_autocomplete.py -v` → 7 passed

- [ ] **Step 5: Commit**

```bash
git add webapp tests/unit/test_autocomplete.py
git commit -m "feat: add stock autocomplete index (webapp.autocomplete)"
```

---

### Task 4: webapp/tasks.py — 串行长任务执行器

背景：AI 分析耗时数十秒。设计约束「同一时刻只允许 1 个分析任务」→ 单 worker 线程 + 内存任务表；`submit` 在已有任务进行时拒绝新任务（返回 `None`）。

**Files:**
- Create: `webapp/tasks.py`
- Test: `tests/unit/test_tasks.py`

- [ ] **Step 1: Write failing tests** `tests/unit/test_tasks.py`

```python
"""TaskManager 单元测试"""

import threading
import time

import pytest

from webapp.tasks import TaskManager


def _poll(manager, task_id, timeout=3.0, target=None):
    """轮询任务直到终态（done/failed）或超时"""
    deadline = time.monotonic() + timeout
    while time.monotonic() < deadline:
        status = manager.get(task_id)["status"]
        if status in ("done", "failed"):
            return status
        time.sleep(0.01)
    return "timeout"


class TestSubmitAndGet:
    def test_status_flow(self):
        m = TaskManager()
        task_id = m.submit(lambda: "ok")
        assert task_id is not None
        assert m.get(task_id)["status"] in ("pending", "running")
        assert _poll(m, task_id) == "done"
        assert m.get(task_id)["result"] == "ok"

    def test_failure_gives_error_message(self):
        m = TaskManager()

        def boom():
            raise RuntimeError("模拟失败")

        task_id = m.submit(boom)
        assert _poll(m, task_id) == "failed"
        assert "模拟失败" in m.get(task_id)["error"]

    def test_rejects_new_task_while_busy(self):
        """串行：有一个任务在执行时，新提交被拒绝（返回 None）"""
        m = TaskManager()
        gate = threading.Event()

        def slow():
            gate.wait(timeout=5)
            return "slow-done"

        first = m.submit(slow)
        assert first is not None
        # 等进入 running（worker 已取走）
        time.sleep(0.1)
        assert m.submit(slow) is None

        gate.set()  # 放行第一个
        assert _poll(m, first) == "done"

    def test_can_submit_after_finish(self):
        m = TaskManager()
        t1 = m.submit(lambda: 1)
        assert _poll(m, t1) == "done"
        t2 = m.submit(lambda: 2)
        assert t2 is not None
        assert _poll(m, t2) == "done"


def test_unknown_task_returns_none():
    m = TaskManager()
    assert m.get("nope") is None
```

- [ ] **Step 2: Run & verify fail**

Run: `python -m pytest tests/unit/test_tasks.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'webapp'`（或 attribute 不存在）

- [ ] **Step 3: Implement** `webapp/tasks.py`

```python
"""webapp.tasks — 串行长任务执行器

单 worker 线程顺序消费队列；任务表保存在内存（重启即清）。
submit 时若已有 pending/running 任务则拒绝（返回 None），
保证同一时刻最多只有一个任务在执行（省 AI Key 并发）。
"""

import logging
import queue
import threading
import time
import uuid
from typing import Any, Callable, Dict, Optional

logger = logging.getLogger(__name__)


class TaskManager:
    def __init__(self) -> None:
        self._tasks: Dict[str, Dict[str, Any]] = {}
        self._lock = threading.Lock()
        self._queue: "queue.Queue[Optional[tuple]]" = queue.Queue()
        self._worker = threading.Thread(target=self._run_loop, daemon=True, name="task-worker")
        self._worker.start()

    # ── 对外接口 ─────────────────────────────────────────

    def submit(self, fn: Callable[[], Any]) -> Optional[str]:
        """提交任务。繁忙时返回 None（拒绝），否则返回 task_id。"""
        with self._lock:
            busy = any(
                t["status"] in ("pending", "running")
                for t in self._tasks.values()
            )
            if busy:
                return None
            task_id = uuid.uuid4().hex[:12]
            self._tasks[task_id] = {
                "status": "pending",
                "progress": None,
                "result": None,
                "error": None,
            }
        self._queue.put((task_id, fn))
        return task_id

    def get(self, task_id: str) -> Optional[Dict[str, Any]]:
        """返回任务快照（副本）或 None"""
        with self._lock:
            return dict(self._tasks.get(task_id) or {})

    # ── 内部 ─────────────────────────────────────────────

    def _run_loop(self) -> None:
        while True:
            task_id, fn = self._queue.get()
            with self._lock:
                self._tasks[task_id]["status"] = "running"
            try:
                result = fn()
            except Exception as exc:
                logger.exception("任务 %s 失败", task_id)
                with self._lock:
                    self._tasks[task_id]["status"] = "failed"
                    self._tasks[task_id]["error"] = str(exc)
            else:
                with self._lock:
                    self._tasks[task_id]["status"] = "done"
                    self._tasks[task_id]["result"] = result
```

- [ ] **Step 4: Run & verify pass**

Run: `python -m pytest tests/unit/test_tasks.py -v` → 5 passed

- [ ] **Step 5: Commit**

```bash
git add webapp/tasks.py tests/unit/test_tasks.py
git commit -m "feat: serial long-running task manager (webapp.tasks)"
```

---

### Task 5: webapp/server.py — FastAPI REST API

**Files:**
- Create: `webapp/server.py`
- Test: `tests/unit/test_server_api.py`

前置约定：
- 报告期 period 格式 `YYYY-MM-DD`，从月份推断类型：3/9 月=季报、6 月=半年报、12 月=年报
- 本地文件名复用 `ReportDownloader.build_filename`（`长江电力_600900_年报_2025.pdf`），公司名从 `StockIndex.company_name()` 取
- 组件为模块级单例；测试用 `monkeypatch.setattr(server, "<名字>", fake)` 替换
- note: 第一步测试里 `fake_analyzer.qa.return_value = "测试 AI 回答"`，与 `test_chat_answer_and_history` 中 `assert r1.json()["answer"] == "测试 AI 回答"` 保持一致

- [ ] **Step 1: Write failing tests** `tests/unit/test_server_api.py`

```python
"""webapp.server API 测试（TestClient + monkeypatch 替换模块级组件）"""

import os
import threading
import time
from datetime import date
from unittest.mock import MagicMock

import pytest
from fastapi.testclient import TestClient

import webapp.server as server
from financial_report_fetcher.models import DownloadStatus, ReportMeta, ReportType


def _meta(code, rt, period, name="测试公司"):
    return ReportMeta(
        company_id=code,
        company_name=name,
        report_type=rt,
        period=period,
        download_url="http://cninfo.example/x.pdf",
        title=f"{name}{period.year}年度报告",
    )


class _FakeReport:
    """模拟 AnalysisReport，只实现 Web 侧用到的接口"""

    def to_json(self):
        return {
            "meta": {"company": "长江电力（600900）", "report_year": 2025},
            "dimensions": [
                {"id": "financial_summary", "name": "财务摘要", "content": "营收 500 亿"}
            ],
        }

    def save(self, output_dir):
        return os.path.join(output_dir, "长江电力_600900_2025_分析报告.md")


class FakeIndex:
    """模拟 StockIndex（方法面足够即可，不继承）"""

    def start(self):
        pass

    def wait_ready(self, timeout=5.0):
        return True

    def search(self, q, limit=10):
        return [{"code": "600900", "name": "长江电力"}] if q == "长江" else []

    def company_name(self, code):
        return "长江电力" if code == "600900" else None

    def is_valid_code(self, code):
        return code == "600900"

    @property
    def is_ready(self):
        return True


@pytest.fixture()
def env(monkeypatch):
    """替换 server 模块级组件为假实现；yield 组件引用供断言"""
    fake_ds = MagicMock()
    fake_ds.fetch_reports.return_value = [
        _meta("600900", ReportType.ANNUAL, date(2025, 12, 31), "长江电力"),
        _meta("600900", ReportType.QUARTERLY, date(2025, 3, 31), "长江电力"),
    ]

    fake_ai = MagicMock()
    fake_ai.api_key = "sk-test"

    fake_analyzer = MagicMock()
    fake_analyzer.analyze.return_value = _FakeReport()
    fake_analyzer.qa.return_value = "测试 AI 回答"

    fake_dl = MagicMock()
    fake_dl.download_one.return_value = DownloadStatus.SUCCESS

    monkeypatch.setattr(server, "datasource", fake_ds)
    monkeypatch.setattr(server, "stock_index", FakeIndex())
    monkeypatch.setattr(server, "ai_client", fake_ai)
    monkeypatch.setattr(server, "analyzer", fake_analyzer)
    monkeypatch.setattr(server, "downloader", fake_dl)
    monkeypatch.setattr(server, "task_manager", server.TaskManager())
    monkeypatch.setattr(server, "chat_sessions", {})

    return {
        "fake_ds": fake_ds,
        "fake_ai": fake_ai,
        "fake_analyzer": fake_analyzer,
        "fake_dl": fake_dl,
    }


@pytest.fixture()
def client(env):
    with TestClient(server.app) as c:
        yield c


class TestAutocomplete:
    def test_search(self, client):
        r = client.get("/api/companies", params={"q": "长江"})
        assert r.status_code == 200
        assert r.json()["results"] == [{"code": "600900", "name": "长江电力"}]


class TestHealth:
    def test_health(self, client):
        r = client.get("/api/health")
        assert r.status_code == 200
        data = r.json()
        assert data["ai_key_configured"] is True
        assert data["index_ready"] is True


class TestReportsList:
    def test_list_reports_desc_order_and_downloaded_flag(self, client):
        r = client.get(
            "/api/companies/600900/reports",
            params={"start": "2025-01-01", "end": "2025-12-31"},
        )
        assert r.status_code == 200
        body = r.json()
        assert body["name"] == "长江电力"
        # 按报告期降序
        assert [x["period"] for x in body["reports"]] == ["2025-12-31", "2025-03-31"]
        assert body["reports"][0]["type"] == "annual"
        # 本地 reports/ 无此文件 → downloaded=False
        assert all(x["downloaded"] is False for x in body["reports"])

    def test_unknown_code_404(self, client):
        r = client.get(
            "/api/companies/999999/reports",
            params={"start": "2025-01-01", "end": "2025-12-31"},
        )
        assert r.status_code == 404

    def test_bad_date_range_400(self, client):
        r = client.get(
            "/api/companies/600900/reports",
            params={"start": "2026-01-01", "end": "2025-12-31"},
        )
        assert r.status_code == 400


class TestServePdf:
    def test_serve_pdf(self, client, tmp_path, monkeypatch):
        pdf = tmp_path / "fake.pdf"
        pdf.write_bytes(b"%PDF-1.4 fake pdf")
        # 跳过真实下载/文件检查，直接返回构造的 PDF
        monkeypatch.setattr(server, "_ensure_pdf", lambda meta: str(pdf))
        r = client.get("/api/reports/600900/2025-12-31.pdf")
        assert r.status_code == 200
        assert r.headers["content-type"] == "application/pdf"
        assert r.content == b"%PDF-1.4 fake pdf"


class TestAnalyze:
    def _poll_until_done(self, client, task_id):
        deadline = time.monotonic() + 5.0
        while time.monotonic() < deadline:
            task = client.get(f"/api/tasks/{task_id}").json()
            if task["status"] in ("done", "failed"):
                return task
            time.sleep(0.03)
        return {"status": "timeout"}

    def test_analyze_submits_task_and_polls(self, client, env):
        assert env  # 使用 env 组件（fake_analyzer）
        r = client.post(
            "/api/reports/600900/2025-12-31/analyze",
            json={"dimensions": ["financial_summary"]},
        )
        assert r.status_code == 200
        task_id = r.json()["task_id"]

        task = self._poll_until_done(client, task_id)
        assert task["status"] == "done"
        result = task["result"]
        assert result["dimensions"][0]["name"] == "财务摘要"
        assert result["markdown_path"].endswith(".md")

    def test_analyze_rejected_when_busy(self, client):
        gate = threading.Event()

        def blocking():
            gate.wait(timeout=10)
            return "released"

        # 先占住 worker
        tid = server.task_manager.submit(blocking)
        assert tid is not None
        time.sleep(0.1)  # 等 worker 取走进入 running

        r = client.post(
            "/api/reports/600900/2025-12-31/analyze",
            json={"dimensions": []},
        )
        assert r.status_code == 409

        gate.set()  # 放行阻塞任务，避免遗留占用

    def test_analyze_requires_ai_key(self, client, env):
        env["fake_ai"].api_key = ""
        r = client.post(
            "/api/reports/600900/2025-12-31/analyze",
            json={"dimensions": []},
        )
        assert r.status_code == 400
        assert "AI_API_KEY" in r.json()["detail"]


class TestChat:
    def test_chat_answer_and_history(self, client, env):
        # 第一次提问后，第二次调用应带上历史（qa 收到的 history 非空）
        client.post(
            "/api/reports/600900/2025-12-31/chat",
            json={"question": "现金流如何？"},
        )
        r2 = client.post(
            "/api/reports/600900/2025-12-31/chat",
            json={"question": "再细说下？"},
        )
        assert r2.status_code == 200
        assert r2.json()["answer"] == "测试 AI 回答"
        last_call = env["fake_analyzer"].qa.call_args
        assert len(last_call[0][1]) == 0 or len(env["fake_analyzer"].qa.call_args[0][2]) >= 2

    def test_chat_requires_ai_key(self, client, env):
        env["fake_ai"].api_key = ""
        r = client.post(
            "/api/reports/600900/2025-12-31/chat",
            json={"question": "x"},
        )
        assert r.status_code == 400
        assert "AI_API_KEY" in r.json()["detail"]

    def test_chat_empty_question_400(self, client):
        r = client.post(
            "/api/reports/600900/2025-12-31/chat",
            json={"question": "   "},
        )
        assert r.status_code == 400
```

- [ ] **Step 2: Run & verify fail**

Run: `python -m pytest tests/unit/test_server_api.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'webapp'`（或 import server 失败，server 尚未实现）

- [ ] **Step 3: Implement** `webapp/server.py`

```python
"""webapp.server — FastAPI 应用与 REST API

对外 API（详见设计文档 3.2）：
- GET  /api/health                            AI Key 与索引就绪状态
- GET  /api/companies?q=                     自动补全
- GET  /api/companies/{code}/reports         财报列表（含本地已下载标记）
- GET  /api/reports/{code}/{period}.pdf      PDF 文件流（iframe 预览）
- POST /api/reports/{code}/{period}/analyze  启动分析任务
- GET  /api/tasks/{task_id}                  任务状态轮询
- POST /api/reports/{code}/{period}/chat     对财报自由问答

设计要点：组件为模块级单例（测试可替换）；分析为串行后台任务；
问答会话保存在内存 dict（重启即清）。
"""

import datetime as dt
import logging
import os
from typing import Any, Dict, List

from fastapi import FastAPI, HTTPException, Query
from fastapi.responses import FileResponse
from fastapi.staticfiles import StaticFiles
from pydantic import BaseModel

from financial_report_fetcher.ai_client import AIClient
from financial_report_fetcher.analyzer import ANALYSIS_TEMPLATES, ReportAnalyzer
from financial_report_fetcher.datasource import CNINFODatasource
from financial_report_fetcher.downloader import ReportDownloader
from financial_report_fetcher.models import DownloadStatus, ReportMeta, ReportType

from .autocomplete import StockIndex
from .tasks import TaskManager

logger = logging.getLogger(__name__)

BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
STATIC_DIR = os.path.join(os.path.dirname(os.path.abspath(__file__)), "static")
REPORTS_DIR = os.path.join(BASE_DIR, "reports")
ANALYSIS_DIR = os.path.join(REPORTS_DIR, "analysis")

app = FastAPI(title="财报分析工具")

# ── 共享组件（模块级单例；测试用 monkeypatch 替换同名模块变量）──
datasource = CNINFODatasource()
stock_index = StockIndex(datasource)
ai_client = AIClient()
analyzer = ReportAnalyzer(ai_client)
downloader = ReportDownloader()
task_manager = TaskManager()
chat_sessions: Dict[str, List[Dict[str, str]]] = {}


# ── 请求体模型 ─────────────────────────────────────────────

class AnalyzeRequest(BaseModel):
    dimensions: List[str] = list(ReportAnalyzer.DEFAULT_DIMENSIONS)


class ChatRequest(BaseModel):
    question: str


# ── 工具函数 ───────────────────────────────────────────────

def _report_type_for_period(period: dt.date) -> ReportType:
    """按报告期月推断财报类型：3/9 月=季报，6 月=半年报，12 月=年报"""
    if period.month == 6:
        return ReportType.SEMI_ANNUAL
    if period.month in (3, 9):
        return ReportType.QUARTERLY
    return ReportType.ANNUAL


def _parse_period(value: str) -> dt.date:
    try:
        return dt.date.fromisoformat(value)
    except ValueError:
        raise HTTPException(400, f"报告期格式无效：{value}（应为 YYYY-MM-DD）")


def _find_report_meta(code: str, period: dt.date) -> ReportMeta:
    """查询单份财报元信息（含公司名与下载地址）；查无 → 404"""
    rt = _report_type_for_period(period)
    reports = datasource.fetch_reports(
        stock_code=code,
        report_types=[rt],
        start_date=period,
        end_date=period,
    )
    if not reports:
        raise HTTPException(
            404, f"{code} 在 {period.isoformat()} 无 {rt.value} 财报"
        )
    return min(reports, key=lambda r: abs((r.period - period).days))


def _local_pdf_path(meta: ReportMeta) -> str:
    return os.path.join(REPORTS_DIR, ReportDownloader.build_filename(meta))


def _require_ai() -> None:
    if not ai_client.api_key:
        raise HTTPException(400, "未配置 AI_API_KEY，请设置环境变量后重试")


def _ensure_pdf(meta: ReportMeta) -> str:
    """本地未下载则先下载；返回本地路径，失败抛 503"""
    path = _local_pdf_path(meta)
    if os.path.exists(path) and os.path.getsize(path) > 0:
        return path
    try:
        status = downloader.download_one(meta, REPORTS_DIR)
        if status != DownloadStatus.SUCCESS:
            raise RuntimeError(f"下载返回 {status.value}")
    except Exception as exc:
        logger.exception("PDF 下载失败：%s", meta.title)
        raise HTTPException(503, f"PDF 下载失败：{exc}")
    return path


# ── 健康检查 ───────────────────────────────────────────────

@app.get("/api/health")
def health() -> Dict[str, Any]:
    return {
        "ai_key_configured": bool(ai_client.api_key),
        "index_ready": stock_index.is_ready,
    }


# ── 自动补全 ───────────────────────────────────────────────

@app.get("/api/companies")
def autocomplete(q: str = Query(default="", max_length=30)) -> Dict[str, Any]:
    stock_index.start()                   # 幂等：确保后台构建已启动
    stock_index.wait_ready(timeout=5.0)   # 首次请求等待索引（实测约 1 秒）
    return {"results": stock_index.search(q, limit=10)}


# ── 财报列表 ───────────────────────────────────────────────

@app.get("/api/companies/{code}/reports")
def list_reports(
    code: str, start: str = Query(...), end: str = Query(...)
) -> Dict[str, Any]:
    start_date, end_date = _parse_period(start), _parse_period(end)
    if start_date > end_date:
        raise HTTPException(400, "start 不得晚于 end")

    stock_index.start()
    if not stock_index.wait_ready(timeout=5.0):
        raise HTTPException(503, "股票索引尚未就绪，请稍后重试")
    if not stock_index.is_valid_code(code):
        raise HTTPException(404, f"未知股票代码：{code}")

    reports = datasource.fetch_reports(
        stock_code=code,
        report_types=[
            ReportType.ANNUAL,
            ReportType.SEMI_ANNUAL,
            ReportType.QUARTERLY,
        ],
        start_date=start_date,
        end_date=end_date,
    )
    reports.sort(key=lambda r: r.period, reverse=True)
    items = [
        {
            "code": r.company_id,
            "period": r.period.isoformat(),
            "type": r.report_type.value,
            "title": r.title,
            "downloaded": os.path.exists(_local_pdf_path(r)),
        }
        for r in reports
    ]
    return {
        "code": code,
        "name": stock_index.company_name(code) or code,
        "reports": items,
    }


# ── PDF 预览 ───────────────────────────────────────────────

@app.get("/api/reports/{code}/{period}.pdf")
def serve_pdf(code: str, period: str) -> FileResponse:
    p = _parse_period(period)
    meta = _find_report_meta(code, p)
    path = _ensure_pdf(meta)
    return FileResponse(
        path, media_type="application/pdf", filename=os.path.basename(path)
    )


# ── 分析任务 ───────────────────────────────────────────────

@app.post("/api/reports/{code}/{period}/analyze")
def analyze_report(code: str, period: str, body: AnalyzeRequest) -> Dict[str, Any]:
    _require_ai()
    p = _parse_period(period)
    meta = _find_report_meta(code, p)

    # 仅保留已知且带预设提示词的维度；过滤后为空则回退默认三维度
    dims = [d for d in body.dimensions if ANALYSIS_TEMPLATES.get(d, {}).get("prompt")]
    if not dims:
        dims = list(ReportAnalyzer.DEFAULT_DIMENSIONS)

    def _run() -> Dict[str, Any]:
        path = _ensure_pdf(meta)
        report = analyzer.analyze(
            path,
            dimensions=dims,
            meta={
                "ticker": meta.company_id,
                "year": p.year,
                "company": meta.company_name,
            },
        )
        md_path = report.save(ANALYSIS_DIR)  # 落盘 reports/analysis/（与 CLI 互通）
        data = report.to_json()
        data["markdown_path"] = md_path
        return data

    task_id = task_manager.submit(_run)
    if task_id is None:
        raise HTTPException(409, "已有分析任务进行中，请稍候")
    return {"task_id": task_id}


# ── 任务轮询 ───────────────────────────────────────────────

@app.get("/api/tasks/{task_id}")
def get_task(task_id: str) -> Dict[str, Any]:
    t = task_manager.get(task_id)
    if t is None:
        raise HTTPException(404, f"未知任务：{task_id}")
    return t


# ── 自由问答 ───────────────────────────────────────────────

@app.post("/api/reports/{code}/{period}/chat")
def chat(code: str, period: str, body: ChatRequest) -> Dict[str, Any]:
    _require_ai()
    if not body.question.strip():
        raise HTTPException(400, "问题不能为空")
    p = _parse_period(period)
    meta = _find_report_meta(code, p)
    path = _ensure_pdf(meta)

    session_key = f"{code}:{p.isoformat()}"
    history = chat_sessions.get(session_key, [])
    answer = analyzer.qa(path, body.question, history=history)
    history = history + [
        {"role": "user", "content": body.question},
        {"role": "assistant", "content": answer},
    ]
    chat_sessions[session_key] = history[-8:]  # 保留最近 4 轮
    return {"answer": answer}


# ── 静态页面 ───────────────────────────────────────────────

@app.get("/")
def index() -> FileResponse:
    return FileResponse(os.path.join(STATIC_DIR, "index.html"))


if os.path.isdir(STATIC_DIR):
    app.mount("/static", StaticFiles(directory=STATIC_DIR), name="static")
```

- [ ] **Step 4: Run tests & verify pass**

Run: `python -m pytest tests/unit/test_server_api.py -v`
Expected: 全部 passed（autocomplete / health / reports / serve_pdf / analyze / chat）

- [ ] **Step 5: 全量回归**

Run: `python -m pytest -q` → 既有 + 新增全部通过

- [ ] **Step 6: Commit**

```bash
git add webapp/server.py tests/unit/test_server_api.py
git commit -m "feat: add fastapi rest api for reports, analysis and chat"
```
### Task 6: 前端单页（static/index.html + app.js + style.css）

**Files:**
- Create: `webapp/static/index.html`
- Create: `webapp/static/style.css`
- Create: `webapp/static/app.js`

- [ ] **Step 1: 编写 index.html**（三栏布局，结构见设计文档第 4 节）

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>财报分析工具</title>
  <link rel="stylesheet" href="/static/style.css" />
</head>
<body>
  <header class="topbar">
    <h1>📊 财报分析工具</h1>
    <span id="ai-status" class="badge badge-warn">AI 未连接</span>
  </header>

  <section class="searchbar">
    <div class="searchbox">
      <input id="stock-input" type="text" placeholder="输入股票名称或代码，如：长江、600900" autocomplete="off" />
      <div id="suggestions" class="suggestions hidden"></div>
    </div>
    <label class="year-range">年份
      <input id="year-start" type="number" value="2023" min="1990" max="2030" />
      ~
      <input id="year-end" type="number" value="2025" min="1990" max="2030" />
    </label>
    <button id="search-btn" class="btn primary">查询</button>
  </section>

  <div id="error-banner" class="banner hidden"></div>

  <main class="layout">
    <aside class="panel reports-panel">
      <h2>财报列表 <small id="stock-label"></small></h2>
      <div id="report-list" class="report-list">
        <p class="hint">先从上方选择股票开始</p>
      </div>
    </aside>

    <section class="panel preview-panel">
      <h2>PDF 预览 <small id="preview-title"></small></h2>
      <iframe id="pdf-frame" class="pdf-frame" src="about:blank" title="财报预览"></iframe>
    </section>

    <aside class="panel analysis-panel">
      <h2>AI 分析</h2>
      <div class="analyze-box">
        <div class="dim-checkboxes" id="dims">
          <label><input type="checkbox" value="financial_summary" checked /> 财务摘要</label>
          <label><input type="checkbox" value="risk_warning" checked /> 风险识别</label>
          <label><input type="checkbox" value="business_highlights" checked /> 经营亮点</label>
        </div>
        <button id="analyze-btn" class="btn primary" disabled>开始分析</button>
        <div id="analyze-result"></div>
      </div>
      <div class="qa-box">
        <h3>自由问答</h3>
        <div id="qa-history" class="qa-history"></div>
        <div class="qa-input-row">
          <input id="qa-input" type="text" placeholder="针对当前财报提问…（先选中左侧财报）" disabled />
          <button id="qa-btn" class="btn" disabled>发送</button>
        </div>
      </div>
    </aside>
  </main>

  <script src="/static/app.js"></script>
</body>
</html>
```

- [ ] **Step 2: 编写 style.css**（完整样式，含三栏布局与各组件状态）

```css
/* 财报分析工具 — 样式 */
:root {
  --bg: #f5f7fa;
  --panel: #ffffff;
  --border: #e2e8f0;
  --text: #1f2937;
  --muted: #6b7280;
  --primary: #2563eb;
  --primary-dark: #1d4ed8;
  --ok: #16a34a;
  --warn: #d97706;
  --danger: #dc2626;
  --radius: 10px;
}

* { box-sizing: border-box; margin: 0; padding: 0; }

html, body { height: 100%; }

body {
  font-family: -apple-system, "PingFang SC", "Microsoft YaHei", "Helvetica Neue", sans-serif;
  background: var(--bg);
  color: var(--text);
  display: flex;
  flex-direction: column;
}

/* ── 顶栏 ── */
.topbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12px 20px;
  background: var(--panel);
  border-bottom: 1px solid var(--border);
}
.topbar h1 { font-size: 18px; font-weight: 600; }
.badge { font-size: 12px; padding: 4px 12px; border-radius: 999px; }
.badge-ok { background: #dcfce7; color: var(--ok); }
.badge-warn { background: #fef3c7; color: var(--warn); }

/* ── 搜索栏 ── */
.searchbar {
  display: flex;
  gap: 12px;
  align-items: center;
  padding: 14px 20px;
  background: var(--panel);
  border-bottom: 1px solid var(--border);
  flex-wrap: wrap;
}
.searchbox { position: relative; flex: 1; min-width: 240px; }
.searchbox input {
  width: 100%;
  padding: 9px 12px;
  border: 1px solid var(--border);
  border-radius: var(--radius);
  font-size: 14px;
}
.searchbox input:focus { outline: 2px solid var(--primary); border-color: transparent; }
.year-range { font-size: 13px; color: var(--muted); display: flex; align-items: center; gap: 6px; }
.year-range input {
  width: 74px;
  padding: 9px 8px;
  border: 1px solid var(--border);
  border-radius: var(--radius);
  font-size: 14px;
}

.hidden { display: none !important; }

.suggestions {
  position: absolute;
  top: calc(100% + 4px);
  left: 0; right: 0;
  background: var(--panel);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  box-shadow: 0 8px 20px rgba(0, 0, 0, 0.08);
  z-index: 100;
  max-height: 320px;
  overflow-y: auto;
}
.suggestion {
  padding: 9px 12px;
  cursor: pointer;
  font-size: 14px;
  display: flex;
  justify-content: space-between;
}
.suggestion:hover { background: #eff6ff; }
.suggestion .code { color: var(--muted); font-size: 12px; }

/* ── 按钮 ── */
.btn {
  padding: 9px 16px;
  border: 1px solid var(--border);
  background: var(--panel);
  color: var(--text);
  border-radius: var(--radius);
  cursor: pointer;
  font-size: 14px;
}
.btn.primary { background: var(--primary); border-color: var(--primary); color: #fff; }
.btn.primary:hover { background: var(--primary-dark); }
.btn:disabled { opacity: 0.5; cursor: not-allowed; }

/* ── 错误横幅 ── */
.banner {
  padding: 10px 20px;
  background: #fef2f2;
  color: var(--danger);
  border-bottom: 1px solid #fecaca;
  font-size: 14px;
}

/* ── 三栏布局 ── */
.layout {
  flex: 1;
  display: grid;
  grid-template-columns: 280px 1fr 340px;
  gap: 12px;
  padding: 12px;
  min-height: 0;
}
.panel {
  background: var(--panel);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  display: flex;
  flex-direction: column;
  min-height: 0;
  overflow: hidden;
}
.panel h2 {
  font-size: 14px;
  font-weight: 600;
  padding: 10px 14px;
  border-bottom: 1px solid var(--border);
  flex: none;
}
.panel h2 small { color: var(--muted); font-weight: 400; margin-left: 4px; }
.panel h3 { font-size: 13px; font-weight: 600; margin-bottom: 6px; }
.panel > .analyze-box, .panel > .qa-box { padding: 0 14px 14px; }

/* ── 财报列表 ── */
.report-list { flex: 1; overflow-y: auto; padding: 6px; }
.report-item {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 10px;
  border-radius: 6px;
  cursor: pointer;
  font-size: 13px;
  border: 1px solid transparent;
}
.report-item:hover { background: #f1f5f9; }
.report-item.selected { background: #eff6ff; border-color: #bfdbfe; }
.tag { font-size: 11px; padding: 2px 8px; border-radius: 999px; white-space: nowrap; }
.tag-annual { background: #dcfce7; color: #166534; }
.tag-semi_annual { background: #fef3c7; color: #92400e; }
.tag-quarterly { background: #e0e7ff; color: #3730a3; }
.report-item .title { flex: 1; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.report-item .mark { color: var(--ok); font-size: 12px; }
.hint { color: var(--muted); font-size: 13px; padding: 14px 12px; text-align: center; }

/* ── PDF 预览 ── */
.pdf-frame { flex: 1; width: 100%; border: 0; }

/* ── 分析面板 ── */
.analysis-panel { overflow-y: auto; }
.dim-checkboxes {
  display: flex;
  flex-direction: column;
  gap: 6px;
  margin: 12px 0;
  font-size: 13px;
}
#analyze-btn { width: 100%; margin-bottom: 12px; }
.dim-card {
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 10px 12px;
  margin-bottom: 10px;
  font-size: 13px;
  line-height: 1.7;
}
.dim-card h4 { margin-bottom: 6px; color: var(--primary); }
.dim-card blockquote {
  border-left: 3px solid var(--border);
  margin: 6px 0;
  padding-left: 10px;
  color: var(--muted);
}
.dim-card ul { padding-left: 18px; margin: 4px 0; }
.error-card {
  border: 1px solid #fecaca;
  background: #fef2f2;
  color: var(--danger);
  border-radius: 8px;
  padding: 10px 12px;
  font-size: 13px;
}

/* ── 问答 ── */
.qa-box { border-top: 1px solid var(--border); padding-top: 12px; }
.qa-history {
  max-height: 280px;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  gap: 8px;
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 10px;
  margin-bottom: 8px;
}
.qa-msg { font-size: 13px; line-height: 1.6; padding: 8px 10px; border-radius: 8px; max-width: 94%; }
.qa-msg.user { align-self: flex-end; background: var(--primary); color: #fff; }
.qa-msg.assistant { align-self: flex-start; background: #f1f5f9; }
.qa-input-row { display: flex; gap: 8px; }
.qa-input-row input {
  flex: 1;
  padding: 9px 12px;
  border: 1px solid var(--border);
  border-radius: var(--radius);
  font-size: 13px;
}
```

- [ ] **Step 3: 编写 app.js**（原生 JS，无依赖）

```javascript
// 财报分析工具 — 前端逻辑（原生 JS，无依赖）
const state = {
  selected: null,        // {code, name}
  selectedReport: null,  // {period, type, title}
  analyzing: false,
  aiConfigured: false,
};

const $ = (sel) => document.querySelector(sel);

// ── 自动补全 ──
let debounceTimer = null;
$('#stock-input').addEventListener('input', (e) => {
  clearTimeout(debounceTimer);
  const q = e.target.value.trim();
  if (!q) { $('#suggestions').classList.add('hidden'); return; }
  debounceTimer = setTimeout(async () => {
    try {
      const res = await fetch(`/api/companies?q=${encodeURIComponent(q)}`);
      const data = await res.json();
      renderSuggestions(data.results || []);
    } catch (_) { /* 忽略网络错误 */ }
  }, 300);
});

function renderSuggestions(list) {
  const box = $('#suggestions');
  if (!list.length) { box.classList.add('hidden'); return; }
  box.innerHTML = list.map((s) =>
    `<div class="suggestion" data-code="${s.code}" data-name="${s.name}">` +
    `<span>${s.name}</span><span class="code">${s.code}</span></div>`
  ).join('');
  box.classList.remove('hidden');
}

$('#suggestions').addEventListener('click', (e) => {
  const el = e.target.closest('.suggestion');
  if (!el) return;
  selectStock(el.dataset.code, el.dataset.name);
});

async function selectStock(code, name) {
  state.selected = { code, name };
  $('#stock-input').value = `${name} ${code}`;
  $('#suggestions').classList.add('hidden');
  await loadReports();
}

// ── 查询财报列表 ──
$('#search-btn').addEventListener('click', loadReports);

async function loadReports() {
  const val = $('#stock-input').value.trim();
  let name = '', code = '';
  val.split(/[\s,，]+/).forEach((p) => {
    if (/^\d{6}$/.test(p)) code = p;
    else if (p) name = p;
  });
  if (!code) { showError('请输入 6 位股票代码（如 600900）或从下拉中选择'); return; }
  state.selected = { code, name: name || code };

  const start = `${$('#year-start').value}-01-01`;
  const end = `${$('#year-end').value}-12-31`;
  clearError();
  try {
    const res = await fetch(`/api/companies/${code}/reports?start=${start}&end=${end}`);
    const data = await res.json();
    if (!res.ok) { showError(`查询失败（${res.status}）：${data.detail || ''}`); return; }
    state.selected.name = data.name || state.selected.name;
    $('#stock-label').textContent = `（${data.name || code}）`;
    renderReports(data.reports || []);
  } catch (err) { showError(`查询出错：${err.message}`); }
}

const TYPE_LABEL = { annual: '年报', semi_annual: '半年报', quarterly: '季报' };

function renderReports(list) {
  const typeOrder = ['annual', 'semi_annual', 'quarterly'];
  const sorted = [...list].sort((a, b) => b.period.localeCompare(a.period));
  sorted.sort((a, b) => typeOrder.indexOf(a.type) - typeOrder.indexOf(b.type));
  $('#report-list').innerHTML = sorted.map((r) => `
      <div class="report-item" data-period="${r.period}">
        <span class="tag tag-${r.type}">${TYPE_LABEL[r.type] || r.type}</span>
        <span class="title">${r.title || r.period}</span>
        <span class="mark">${r.downloaded ? '✓' : '↧'}</span>
      </div>`).join('');
  $('#report-list').querySelectorAll('.report-item').forEach((el) =>
    el.addEventListener('click', () => selectReport(el.dataset.period))
  );
}

async function selectReport(period) {
  if (!state.selected) return;
  $('#report-list').querySelectorAll('.report-item').forEach((el) =>
    el.classList.toggle('selected', el.dataset.period === period));
  state.selectedReport = { period };
  $('#preview-title').textContent = `${state.selected.name} ${period}`;
  $('#pdf-frame').src = `/api/reports/${state.selected.code}/${period}.pdf`;
  updateAnalysisState();
}

// ── 健康状态 ──
async function init() {
  try {
    const res = await fetch('/api/health');
    const h = await res.json();
    state.aiConfigured = h.ai_key_configured;
    $('#ai-status').textContent = h.ai_key_configured ? 'AI 已连接' : 'AI 未配置（设置 AI_API_KEY）';
    $('#ai-status').className = 'badge ' + (h.ai_key_configured ? 'badge-ok' : 'badge-warn');
    updateAnalysisState();
  } catch (_) { /* 服务未启动时忽略 */ }
}

function updateAnalysisState() {
  const ready = state.aiConfigured && !!state.selectedReport && !state.analyzing;
  $('#analyze-btn').disabled = !ready;
  $('#qa-btn').disabled = !ready;
  $('#qa-input').disabled = !ready;
}

// ── 分析任务 ──
$('#analyze-btn').addEventListener('click', async () => {
  if (!state.selectedReport || state.analyzing) return;
  state.analyzing = true;
  updateAnalysisState();
  $('#analyze-result').innerHTML = '<div class="hint">任务已提交，等待执行…</div>';
  const dims = [...document.querySelectorAll('#dims input:checked')].map((i) => i.value);
  try {
    const res = await fetch(
      `/api/reports/${state.selected.code}/${state.selectedReport.period}/analyze`,
      { method: 'POST', headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ dimensions: dims }) });
    const data = await res.json();
    if (!res.ok) throw new Error(data.detail || `HTTP ${res.status}`);
    pollTask(data.task_id);
  } catch (err) {
    $('#analyze-result').innerHTML = `<div class="error-card">${escapeHtml(err.message)}</div>`;
    state.analyzing = false;
    updateAnalysisState();
  }
});

async function pollTask(taskId) {
  const res = await fetch(`/api/tasks/${taskId}`);
  const t = await res.json();
  if (t.status === 'running' || t.status === 'pending') {
    $('#analyze-result').innerHTML = '<div class="hint">分析进行中…（逐维度请求 AI，通常 1~3 分钟）</div>';
    setTimeout(() => pollTask(taskId), 2000);
    return;
  }
  state.analyzing = false;
  updateAnalysisState();
  if (t.status === 'failed') {
    $('#analyze-result').innerHTML =
      `<div class="error-card">分析失败：${escapeHtml(t.error || '未知错误')}</div>`;
    return;
  }
  const report = t.result || {};
  $('#analyze-result').innerHTML = renderReport(report);
  if (report.markdown_path) {
    $('#analyze-result').insertAdjacentHTML('afterbegin',
      `<div class="hint" style="text-align:left">✅ 已保存：${escapeHtml(report.markdown_path)}</div>`);
  }
}

function renderReport(report) {
  const dims = (report.dimensions || [])
    .map((d) => `<div class="dim-card"><h4>${escapeHtml(d.name || '')}</h4>` +
      `<div>${d.error
        ? `<span class="error-card">${escapeHtml(d.error)}</span>`
        : renderMarkdown(d.content || '')}</div></div>`)
    .join('');
  return dims || '<div class="hint">无分析结果</div>';
}

// ── 简易 Markdown 渲染（标题/加粗/列表/引用/换行）──
function renderMarkdown(text) {
  const esc = escapeHtml(String(text));
  const lines = esc.split('\n');
  let inList = false;
  const html = lines.map((line) => {
    const h = line.match(/^(#{1,4})\s/);
    if (h) {
      const level = Math.min(h[1].length, 4);
      const close = inList ? '</ul>' : '';
      inList = false;
      return close + `<h${level}>${line.slice(h[1].length + 1)}</h${level}>`;
    }
    if (/^\s*[-*]\s+/.test(line)) {
      const open = inList ? '' : '<ul>';
      inList = true;
      return open + `<li>${line.replace(/^\s*[-*]\s*/, '')}</li>`;
    }
    const close = inList ? '</ul>' : '';
    inList = false;
    if (/^\s*>\s?/.test(line)) return close + `<blockquote>${line.replace(/^\s*>\s?/, '')}</blockquote>`;
    if (line.trim() === '') return close + '<br>';
    return close + line.replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>');
  }).join('\n');
  return html + (inList ? '</ul>' : '');
}

// ── 自由问答 ──
$('#qa-btn').addEventListener('click', sendQuestion);
$('#qa-input').addEventListener('keydown', (e) => { if (e.key === 'Enter') sendQuestion(); });

async function sendQuestion() {
  const q = $('#qa-input').value.trim();
  if (!q || !state.selectedReport) return;
  $('#qa-input').value = '';
  appendQa('user', q);
  try {
    const res = await fetch(
      `/api/reports/${state.selected.code}/${state.selectedReport.period}/chat`,
      { method: 'POST', headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ question: q }) });
    const data = await res.json();
    if (!res.ok) throw new Error(data.detail || `HTTP ${res.status}`);
    appendQa('assistant', data.answer);
  } catch (err) {
    appendQa('assistant', `⚠️ ${err.message}`);
  }
}

function appendQa(role, content) {
  const box = $('#qa-history');
  const div = document.createElement('div');
  div.className = `qa-msg ${role}`;
  div.innerHTML = role === 'user' ? escapeHtml(content) : renderMarkdown(content);
  box.appendChild(div);
  box.scrollTop = box.scrollHeight;
}

function escapeHtml(s) {
  return String(s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
}

function showError(msg) {
  const b = $('#error-banner');
  b.textContent = `⚠ ${msg}`;
  b.classList.remove('hidden');
}
function clearError() { $('#error-banner').classList.add('hidden'); }

init();
```

- [ ] **Step 4: 手工冒烟验收**

启动：`python -m uvicorn webapp.server:app --port 8000`，浏览器打开 `http://127.0.0.1:8000`，验证（对照设计文档第 8 节验收标准）：
1. 输入「长江」出现下拉建议 → 选中后列表出现 2023-2025 年财报
2. 点击 2025 年报 → iframe 正常渲染 PDF（未下载项先自动下载，标记变为 ✓）
3. 配置 `AI_API_KEY` 后「开始分析」→ 轮询出现三维度分析卡片，`reports/analysis/` 有落盘文件
4. 提问「现金流情况如何？」获得回答并可追问
5. 未配置 `AI_API_KEY` 时：顶栏黄色警告、分析/问答按钮禁用
6. 巨潮接口故障时出现红色横幅 + 可重试

- [ ] **Step 5: Commit**

```bash
git add webapp/static
git commit -m "feat: add single-page frontend (search/report list/pdf preview/analysis/qa)"
```
### Task 7: 依赖、文档与收尾

**Files:**
- Modify: `requirements.txt`
- Create: `README.md`

- [ ] **Step 1: 更新 requirements.txt**

```text
pyyaml>=6.0
requests>=2.28
pydantic>=2.0
tenacity>=8.0
hypothesis>=6.0
pytest>=7.0
fastapi>=0.110
uvicorn>=0.29
pypdf>=4.0
httpx>=0.27
```

然后安装：`pip install -r requirements.txt`

- [ ] **Step 2: 创建 README.md**（项目根目录，简要：功能、CLI 用法、Web 启动方式、配置环境变量；可参照 spec 8 验收标准组织）

- [ ] **Step 3: 最终回归**

Run: `python -m pytest -q` → 全部通过
Run: 手动冒烟（Task 6 Step 4 清单全绿）

- [ ] **Step 4: Commit**

```bash
git add requirements.txt README.md
git commit -m "docs: add README, bump requirements for web stack"
git log --oneline
```

---

## Self-Review 记录

- **Spec 覆盖**：自动补全(T3)、报告列表+预览(T5/T6)、分析维度+轮询(T4/T5/T6)、自由问答+历史(T5/T6)、错误处理(__about 401/409/503 及前端提示)、健康检查(T5、spec 5 的顶栏 AI 状态)、手动验收(T7 Step3)。✓
- **类型一致性**：`ReportMeta.company_name` 字段名在 T5 的 `_find_pdf_meta`/`_run` 中与 `models.py` 定义一致；`report.save()` 返回 md 路径；`DownloadStatus.SUCCESS` 枚举比较一致。分析任务结果统一为 `report.to_json()` dict。
- **差异化决策记录**：CLI `chat()` 保持 stdin 交互不变，Web 问答由 `analyzer.qa()` 提供；spec 3.3 已在 Task 2 Step 6 同步。
