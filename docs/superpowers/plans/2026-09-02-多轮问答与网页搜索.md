# 多轮问答与网页搜索实现计划

> 用户已在本会话确认继续开发并提供运行环境变量 `TAVILY_API_KEY`；直接在 `main` 完成，不创建 worktree。

1. 为 Tavily provider、工具定义回退和环境变量缺失场景编写单元测试；实现无依赖 HTTP provider 与 `web_search` OpenAI function schema。
2. 扩展 `RagQA.answer_stream`：阶段事件、工具总量限制、重复调用保护、网页来源提取与强制最终总结；补多轮和 SSE 顺序测试。
3. 在 `webapp.server` 合并 MCP 与内部网页工具、修复 MCP 回退短路、透传新增 SSE 字段；补 API 测试。
4. 在 `webapp/static/app.js` / `style.css` 渲染实时阶段与网页来源，保持无障碍 live region 和现有对话布局。
5. 更新示例配置/说明，执行定向测试和全量回归；拉取 `origin/main` 后重跑关键测试、提交并推送。
